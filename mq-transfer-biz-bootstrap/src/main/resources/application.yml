spring:
  application:
    name: mq-transfer-biz-bootstrap
cluster:
  mode: cluster         ##cluster,standalone
logging:
  level:
    root: INFO
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss.SSS} [%-5level] [%thread] [%logger{50}] - %msg%n'